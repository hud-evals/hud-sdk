# Copy this file to `.env` and adjust values before launching the Trainium server

# ---------- Model ----------
MODEL_NAME=Qwen/Qwen2.5-7B-Instruct   # Hugging Face model ID
HF_TOKEN=<your_huggingface_token>     # If model is gated/private

# ---------- Neuron runtime ----------
NEURON_RT_NUM_CORES=2                 # trn1.2xlarge has 2 cores
XLA_USE_BF16=1                        # Enable bf16 math
NEURON_COMPILE_CACHE_URL=/home/ubuntu/neuron_cache

# ---------- Server ----------
PORT=8000                             # FastAPI port 